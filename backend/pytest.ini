[pytest]
# Run from the backend/ directory so `security` and `parsing` import as top-level
# modules (matching how main.py imports them).
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    needs_db: integration test that requires a live PostgreSQL database (skipped by default)
